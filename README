CSE 423 JZero Compiler Project

Douglas Newquist

Compiling the program:
	- make
		To build the program normally
	- make debug
		Adds extra debug output to the final executable

Flags supported:
	-none, switches all flags off
	-token, prints out all read tokens
	-tree, prints the parsed syntax tree
	-dot, changes output to .dot file format, tree and symbols supported
	-bison, (debug only) prints bison's debug output
	-symtab, print symbol tables
	-symbols, alias for -symtab

Examples
	./j0 JavaSamples/Hello.java
	./j0 -tree -dot JavaSamples/Hello.java > hello.dot

Test script
	run tests/testrunner.py using python3 with the current working directory being the one that contains this file
